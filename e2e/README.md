# End-to-end tests

Playwright specs live here (`*.spec.ts`). None are written yet — this directory
holds the suite once tests are added.

See the **Testing (e2e)** section in the repo root `CLAUDE.md` for how the test
stack (test database, ports, servers) is wired and how to run it.
